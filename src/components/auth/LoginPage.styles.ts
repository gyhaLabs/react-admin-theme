import { makeStyles } from "@material-ui/core";

export const useAlternativeLoginStyle = makeStyles(() => ({
  signInPanel: {
    background: "#ccc",
    borderTop: "1px dashed #aaa",
    padding: 20,
    marginTop: 20,
  },
}));
